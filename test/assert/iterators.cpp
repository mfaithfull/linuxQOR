// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

//Derived from assertcc
//Copyright 2021 Sean Nash
//under BSD 3 clause license

#include "../../src/configuration/configuration.h"

#include <vector>

#include "../../src/qor/test/test.h"
#include "../../src/qor/assert/assertcc.h"

namespace test_iterators {

    using namespace qor;
    using namespace qor::test;

    struct IteratorsTests {};

    qor_pp_test_suite_case(IteratorsTests, IsEmpty) 
    {
        std::vector<int> v0;
        assertThat(v0).isEmpty();
        std::list<int> l0;
        assertThat(l0).isEmpty();
        std::array<int, 0> a0;
        assertThat(a0).isEmpty();
        std::deque<int> d0{};
        assertThat(a0).isEmpty();
        std::forward_list<int> fl{};
        assertThat(fl).isEmpty();
    }

    qor_pp_test_suite_case(IteratorsTests, IsExpectIsEmpty) 
    {
        std::vector<int> v0;
        expectThat(v0).isEmpty();
        std::list<int> l0;
        expectThat(l0).isEmpty();
        std::array<int, 0> a0;
        expectThat(a0).isEmpty();
        std::deque<int> d0{};
        expectThat(a0).isEmpty();
        std::forward_list<int> fl{};
        expectThat(fl).isEmpty();
    }

    qor_pp_test_suite_case(IteratorsTests, IsNotEmpty) 
    {
        std::vector<int> v0{1, 2};
        assertThat(v0).isNotEmpty();
        std::list<int> l0{3, 4};
        assertThat(l0).isNotEmpty();
        std::array<int, 10> a0;
        assertThat(a0).isNotEmpty();
        std::deque<int> d0{1, 2, 3};
        assertThat(d0).isNotEmpty();
        std::forward_list<int> fl{1, 2, 3};
        assertThat(fl).isNotEmpty();
    }

    qor_pp_test_suite_case(IteratorsTests, IsNotEqualTo) 
    {
        std::vector<int> v0{1, 2};
        std::vector<int> v1{1, 3};
        assertThat(v0).isNotEqualTo(v1.begin(), v1.end());
    }

    qor_pp_test_suite_case(IteratorsTests, IsEqualTo) 
    {
        std::vector<int> v0{1, 2};
        std::vector<int> v1{1, 2};
        assertThat(v0).isEqualTo(v1.begin(), v1.end());
    }

    qor_pp_test_suite_case(IteratorsTests, Contains) 
    {
        std::vector<int> v0{1, 2};
        assertThat(v0).contains(2);
    }

    qor_pp_test_suite_case(IteratorsTests, HasSize) 
    {
        std::vector<int> v0{1, 2};
        assertThat(v0).hasSizeThat().isEqualTo(2);
    }

}//test_iterators
