
// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_COMPONENTS_DATA_FORMATS_TTF_RAWFONT
#define QOR_PP_H_COMPONENTS_DATA_FORMATS_TTF_RAWFONT

#include <stdint.h>
#include <string>
#include <map>

#include "src/qor/memory/reference/newref.h"
#include "blobbuffer.h"
#include "tables/headtable.h"
#include "tables/cmaptable.h"
#include "tables/locatable.h"
#include "tables/glyphtable.h"
#include "tables/hheatable.h"
#include "tables/hmtxtable.h"
#include "tables/kerntable.h"
#include "tables/gpostable.h"

namespace qor{ namespace res { namespace ttf {

    class qor_pp_module_interface(QOR_TTF) RawFont
    {
    public:

        RawFont(const std::string& fileName);

        BlobBuffer GetTable(uint32_t tag);
        void Init();

        //add tables - svg; etc

        /*stbtt__buf cff;                  // cff font data
        stbtt__buf charstrings;            // the charstring index
        stbtt__buf gsubrs;                 // global charstring subroutines index
        stbtt__buf subrs;                  // private charstring subroutines index
        stbtt__buf fontdicts;              // array of font dicts
        stbtt__buf fdselect;               // map from glyph to fontdict*/

        uint32_t GetGlyphIndex(CodePoint cp)
        {
            return m_cmapTable.IsNotNull() ? m_cmapTable->GetGlyphIndex(cp.Value()) : 0;
        }

        uint32_t GetGlyphOffset(size_t index)
        {
            return m_locaTable.IsNotNull() && m_headTable.IsNotNull() ? 
                m_locaTable->GetGlyphOffset(index) : 0;
        }

        uint32_t GetCodePointOffset(CodePoint cp)
        {
            return GetGlyphOffset(GetGlyphIndex(cp));
        }

        size_t GetGlyphCount()
        {
            return m_locaTable->GlyphCount();
        }

        uint16_t GetUnitsPerEm()
        {
            return m_headTable->GetUnitsPerEm();
        }

        std::string m_fileName;
        std::map<uint32_t, BlobBuffer> m_tables;

        ref_of<HeadTable>::type m_headTable;
        ref_of<CmapTable>::type m_cmapTable;
        ref_of<LocaTable>::type m_locaTable;
        ref_of<GlyfTable>::type m_glyfTable;
        ref_of<HheaTable>::type m_hheaTable;
        ref_of<HmtxTable>::type m_hmtxTable;
        ref_of<KernTable>::type m_kernTable;
        ref_of<GposTable>::type m_gposTable;
    };

}}}//qor::res::ttf

#endif//QOR_PP_H_COMPONENTS_DATA_FORMATS_TTF_RAWFONT