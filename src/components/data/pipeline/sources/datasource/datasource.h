// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_COMPONENTS_PIPELINE_SOURCES_DATA
#define QOR_PP_H_COMPONENTS_PIPELINE_SOURCES_DATA

#include <string>
#include <type_traits>
#include "src/framework/data/pipeline/podbuffer.h"
#include "src/framework/data/pipeline/source.h"
#include "src/framework/data/pipeline/sink.h"
#include "src/platform/compiler/compiler.h"
#include "src/platform/architecture/cpu.h"
#include "src/framework/event/delegate/delegate.h"

namespace qor{ namespace pipeline{

    class qor_pp_module_interface(QOR_DATASOURCE) DataSource : public pipeline::Source
    {

    public:

        DataSource() = default;
        virtual ~DataSource() = default;
            
        void SetCallback( const Delegate<bool(size_t&, size_t)>& callback)
        {
            m_callBack = callback;
        }

        void SetByteOrder(const arch::Endian byteOrder)
        {
            m_outputByteOrder = byteOrder;
        }

        void InsertBufferValue(byte* space, size_t realWrite, byte* data, size_t size, size_t totalWritten);
        bool Insert(byte* data, size_t byteCount);

        template <typename T> requires std::is_standard_layout_v<T> && std::is_trivially_copyable_v<T>
        inline bool Insert(T value)
        {
            byte* data = reinterpret_cast<byte*>(&value);
            size_t byteCount = sizeof(T);
            return Insert(data, byteCount);
        }
    
        inline bool Read(size_t& unitsRead, size_t unitsToRead = 1) override
        {
            return Pull(unitsRead, unitsToRead) ? Push(unitsRead, unitsRead) : false;        
        }

    protected:
            
        bool Pull(size_t& unitsRead, size_t unitsToRead);

        inline bool Push(size_t& unitsRead, size_t unitsToRead)
        {
            return ( GetFlowMode() == Element::FlowMode::Push ) ? (ActualSink()->Write(unitsRead, unitsToRead) && (unitsRead > 0)) : true;        
        }

        arch::Endian m_outputByteOrder{arch::Endian::network};
        Delegate<bool(size_t&, size_t)> m_callBack;
    };

}}//qor::pipeline

#endif//QOR_PP_H_COMPONENTS_PIPELINE_SOURCES_DATA
