// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#include "src/configuration/configuration.h"

#include "windowstyle.h"

namespace qor{ namespace platform { namespace win{

    bool WindowStyle::IsOverlapped()
    {
        return true;
    }

    bool WindowStyle::IsBorder()
    {
        return (m_style & Border) != 0;
    }

    bool WindowStyle::IsCaption()
    {
        return (m_style & Caption) != 0;
    }

    bool WindowStyle::IsChild()
    {
        return (m_style & Child) != 0;
    }

    bool WindowStyle::IsClipChildren()
    {
        return (m_style & ClipChildren) != 0;
    }

    bool WindowStyle::IsClipSiblings()
    {
        return (m_style & ClipSiblings) != 0;
    }

    bool WindowStyle::IsDisabled()
    {
        return (m_style & Disabled) != 0;
    }

    bool WindowStyle::IsDialogFrame()
    {
        return (m_style & DialogFrame) != 0;
    }

    bool WindowStyle::IsGroup()
    {
        return (m_style & Group) != 0;
    }

    bool WindowStyle::IsHScroll()
    {
        return (m_style & HScroll) != 0;
    }

    bool WindowStyle::IsMaximize()
    {
        return (m_style & Maximize) != 0;
    }

    bool WindowStyle::IsMaximizeBox()
    {
        return (m_style & MaximizeBox) != 0;
    }

    bool WindowStyle::IsMinimize()
    {
        return (m_style & Minimize) != 0;
    }

    bool WindowStyle::IsMinimizeBox()
    {
        return (m_style & MinimizeBox) != 0;
    }

    bool WindowStyle::IsPopup()
    {
        return (m_style & Popup) != 0;
    }

    bool WindowStyle::IsSizingBorder()
    {
        return (m_style & SizingBorder) != 0;
    }

    bool WindowStyle::IsSystemMenu()
    {
        return (m_style & SystemMenu) != 0;
    }

    bool WindowStyle::IsTabStop()
    {
        return IsChild() && ((m_style & TabStop) != 0);
    }

    bool WindowStyle::IsVisible()
    {
        return (m_style & Visible) != 0;
    }

    bool WindowStyle::IsVScroll()
    {
        return (m_style & VScroll) != 0;
    }

    void WindowStyle::SetBorder(bool value)
    {
        m_style = value ? (m_style | Border) : (m_style & ~Border);
    }
    
    void WindowStyle::SetCaption(bool value)
    {
        m_style = value ? (m_style | Caption) : (m_style & ~Caption);
    }

    void WindowStyle::SetChild(bool value)
    {
        if(value)
        {
            SetPopup(false);
        }
        m_style = value ? (m_style | Child) : (m_style & ~Child);
    }

    void WindowStyle::SetClipChildren(bool value)
    {
        m_style = value ? (m_style | ClipChildren) : (m_style & ~ClipChildren);
    }

    void WindowStyle::SetClipSiblings(bool value)
    {
        m_style = value ? (m_style | ClipSiblings) : (m_style & ~ClipSiblings);
    }

    void WindowStyle::SetDisabled(bool value)
    {
        m_style = value ? (m_style | Disabled) : (m_style & ~Disabled);
    }

    void WindowStyle::SetDialogFrame(bool value)
    {
        m_style = value ? (m_style | DialogFrame) : (m_style & ~DialogFrame);
    }

    void WindowStyle::SetGroup(bool value)
    {
        m_style = value ? (m_style | Group) : (m_style & ~Group);
    }

    void WindowStyle::SetHScroll(bool value)
    {
        m_style = value ? (m_style | HScroll) : (m_style & ~HScroll);
    }

    void WindowStyle::SetMaximize(bool value)
    {
        m_style = value ? (m_style | Maximize) : (m_style & ~Maximize);
    }

    void WindowStyle::SetMaximizeBox(bool value)
    {
        if(value)
        {
            SetSystemMenu(true);
        }
        m_style = value ? (m_style | MaximizeBox) : (m_style & ~MaximizeBox);
    }

    void WindowStyle::SetMinimize(bool value)
    {
        m_style = value ? (m_style | Minimize) : (m_style & ~Minimize);
    }

    void WindowStyle::SetMinimizeBox(bool value)
    {
        if(value)
        {
            SetSystemMenu(true);
        }
        m_style = value ? (m_style | MinimizeBox) : (m_style & ~MinimizeBox);
    }

    void WindowStyle::SetPopup(bool value)
    {
        if(value)
        {
            SetChild(false);
        }
        m_style = value ? (m_style | Popup) : (m_style & ~Popup);
    }

    void WindowStyle::SetSizingBorder(bool value)
    {
        m_style = value ? (m_style | SizingBorder) : (m_style & ~SizingBorder);
    }

    void WindowStyle::SetSystemMenu(bool value)
    {
        if(value)
        {
            SetCaption(true);
        }
        m_style = value ? (m_style | SystemMenu) : (m_style & ~SystemMenu);
    }

    void WindowStyle::SetTabStop(bool value)
    {
        m_style = value ? (m_style | TabStop) : (m_style & ~TabStop);
    }

    void WindowStyle::SetVisible(bool value)
    {
        m_style = value ? (m_style | Visible) : (m_style & ~Visible);
    }

    void WindowStyle::SetVScroll(bool value)
    {
        m_style = value ? (m_style | VScroll) : (m_style & ~VScroll);
    }
    
    bool ExtendedWindowStyle::IsAcceptFiles()
    {
        return (m_style & AcceptFiles) != 0;
    }

    bool ExtendedWindowStyle::IsAppWindow()
    {
        return (m_style & AppWindow) != 0;
    }

    bool ExtendedWindowStyle::IsClientEdge()
    {
        return (m_style & ClientEdge) != 0;
    }

    bool ExtendedWindowStyle::IsComposited()
    {
        return (m_style & Composited) != 0;
    }

    bool ExtendedWindowStyle::IsContextHelp()
    {
        return (m_style & ContextHelp) != 0;
    }

    bool ExtendedWindowStyle::IsControlParent()
    {
        return (m_style & ControlParent) != 0;
    }

    bool ExtendedWindowStyle::IsDialogModalFrame()
    {
        return (m_style & DialogModalFrame) != 0;
    }

    bool ExtendedWindowStyle::IsLayered()
    {
        return (m_style & Layered) != 0;
    }

    bool ExtendedWindowStyle::IsLayoutRTL()
    {
        return (m_style & LayoutRTL) != 0;
    }

    bool ExtendedWindowStyle::IsLeftScrollBar()
    {
        return (m_style & LeftScrollBar) != 0;
    }

    bool ExtendedWindowStyle::IsMDIChild()
    {
        return (m_style & MDIChild) != 0;
    }

    bool ExtendedWindowStyle::IsNoActivate()
    {
        return (m_style & NoActivate) != 0;
    }
    
    bool ExtendedWindowStyle::IsNoInheritLayout()
    {
        return (m_style & NoInheritLayout) != 0;
    }

    bool ExtendedWindowStyle::IsNoParentNotify()
    {
        return (m_style & NoParentNotify) != 0;
    }

    bool ExtendedWindowStyle::IsNoRedirectionBitmap()
    {
        return (m_style & NoRedirectionBitmap) != 0;
    }

    bool ExtendedWindowStyle::IsRightAligned()
    {
        return (m_style & RightAligned) != 0;
    }

    bool ExtendedWindowStyle::IsRTLReading()
    {
        return (m_style & RTLReading) != 0;
    }

    bool ExtendedWindowStyle::IsStaticEdge()
    {
        return (m_style & StaticEdge) != 0;
    }

    bool ExtendedWindowStyle::IsToolWindow()
    {
        return (m_style & ToolWindow) != 0;
    }

    bool ExtendedWindowStyle::IsTopMost()
    {
        return (m_style & TopMost) != 0;
    }

    bool ExtendedWindowStyle::IsTransparent()
    {
        return (m_style & Transparent) != 0;
    }

    bool ExtendedWindowStyle::IsWindowEdge()
    {
        return (m_style & WindowEdge) != 0;
    }
    
    void ExtendedWindowStyle::SetAcceptFiles(bool value)
    {
        m_style = value ? (m_style | AcceptFiles) : (m_style & ~AcceptFiles);
    }
    
    void ExtendedWindowStyle::SetAppWindow(bool value)
    {
        m_style = value ? (m_style | AppWindow) : (m_style & ~AppWindow);
    }
    
    void ExtendedWindowStyle::SetClientEdge(bool value)
    {
        m_style = value ? (m_style | ClientEdge) : (m_style & ~ClientEdge);
    }
    
    void ExtendedWindowStyle::SetComposited(bool value)
    {
        m_style = value ? (m_style | Composited) : (m_style & ~Composited);
    }
    
    void ExtendedWindowStyle::SetContextHelp(bool value)
    {
        m_style = value ? (m_style | ContextHelp) : (m_style & ~ContextHelp);
    }
    
    void ExtendedWindowStyle::SetControlParent(bool value)
    {
        m_style = value ? (m_style | ControlParent) : (m_style & ~ControlParent);
    }
    
    void ExtendedWindowStyle::SetDialogModalFrame(bool value)
    {
        m_style = value ? (m_style | DialogModalFrame) : (m_style & ~DialogModalFrame);
    }
    
    void ExtendedWindowStyle::SetLayered(bool value)
    {
        m_style = value ? (m_style | Layered) : (m_style & ~Layered);
    }
    
    void ExtendedWindowStyle::SetLayoutRTL(bool value)
    {
        m_style = value ? (m_style | LayoutRTL) : (m_style & ~LayoutRTL);
    }
    
    void ExtendedWindowStyle::SetLeftScrollBar(bool value)
    {
        m_style = value ? (m_style | LeftScrollBar) : (m_style & ~LeftScrollBar);
    }
    
    void ExtendedWindowStyle::SetMDIChild(bool value)
    {
        m_style = value ? (m_style | MDIChild) : (m_style & ~MDIChild);
    }
    
    void ExtendedWindowStyle::SetNoActivate(bool value)
    {
        m_style = value ? (m_style | NoActivate) : (m_style & ~NoActivate);
    }
    
    void ExtendedWindowStyle::SetNoInheritLayout(bool value)
    {
        m_style = value ? (m_style | NoInheritLayout) : (m_style & ~NoInheritLayout);
    }
    
    void ExtendedWindowStyle::SetNoParentNotify(bool value)
    {
        m_style = value ? (m_style | NoParentNotify) : (m_style & ~NoParentNotify);
    }
    
    void ExtendedWindowStyle::SetNoRedirectionBitmap(bool value)
    {
        m_style = value ? (m_style | NoRedirectionBitmap) : (m_style & ~NoRedirectionBitmap);
    }
    
    void ExtendedWindowStyle::SetRightAligned(bool value)
    {
        m_style = value ? (m_style | RightAligned) : (m_style & ~RightAligned);
    }
    
    void ExtendedWindowStyle::SetRTLReading(bool value)
    {
        m_style = value ? (m_style | RTLReading) : (m_style & ~RTLReading);
    }
    
    void ExtendedWindowStyle::SetStaticEdge(bool value)
    {
        m_style = value ? (m_style | StaticEdge) : (m_style & ~StaticEdge);
    }
    
    void ExtendedWindowStyle::SetToolWindow(bool value)
    {
        m_style = value ? (m_style | ToolWindow) : (m_style & ~ToolWindow);
    }
    
    void ExtendedWindowStyle::SetTopMost(bool value)
    {
        m_style = value ? (m_style | TopMost) : (m_style & ~TopMost);
    }
    
    void ExtendedWindowStyle::SetTransparent(bool value)
    {
        m_style = value ? (m_style | Transparent) : (m_style & ~Transparent);
    }
    
    void ExtendedWindowStyle::SetWindowEdge(bool value)
    {
        m_style = value ? (m_style | WindowEdge) : (m_style & ~WindowEdge);
    }

}}}//qor::platform::win
