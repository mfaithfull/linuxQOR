// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#include "src/configuration/configuration.h"

#include "comboboxcontroller.h"
#include "../../perthread.h"
#include "src/platform/os/windows/ui/gui/view/messages.h"
#include "src/platform/os/windows/common/stringconv.h"
#include "src/platform/os/windows/api_layer/user/user32.h"

using namespace qor::win::api;

#define WINDOW_HANDLE (HWND)(m_window->GetHandle().Use())

namespace qor{ namespace platform { namespace win{

    ComboBoxController::ComboBoxController() : WindowController(){}
    ComboBoxController::ComboBoxController(ref_of<ComboBox>::type combobox) : WindowController(combobox.AsRef<Window>())
    {
        combobox->SetController(this);
    }

    unsigned short ComboBoxController::GetId() const
    {
        return static_cast<unsigned short>(User32::GetDlgCtrlID(WINDOW_HANDLE));
    }

    int ComboBoxController::CompareItem(CompareItemStruct* pCompareItemStruct)
    {
        // The return value indicates the relative position of the two items.
        // It may be any of the values shown in the following table.
        //
        //  Value Meaning
        //  -1 Item 1 precedes item 2 in the sorted order.
        //   0 Items 1 and 2 are equivalent in the sorted order.
        //   1 Item 1 follows item 2 in the sorted order.

        return 0;
    }

    int ComboBoxController::AddString(const tstring& string) const
    {
        return 0;
    }

    void ComboBoxController::Clear() const
    {
        User32::SendMessageT(WINDOW_HANDLE, WM_CLEAR, 0, 0);
    }

    void ComboBoxController::Copy() const
    {
        User32::SendMessageT(WINDOW_HANDLE, WM_COPY, 0, 0);
    }

    void ComboBoxController::Cut() const
    {
        User32::SendMessageT(WINDOW_HANDLE, WM_CUT, 0, 0);
    }

    int ComboBoxController::DeleteString(int index) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_DELETESTRING, wparam, 0));
    }

    int ComboBoxController::Dir(unsigned int attr, const tstring& wildCard ) const
    {
        WPARAM wparam = static_cast<WPARAM>(attr);
        LPARAM lparam = reinterpret_cast<LPARAM>(wildCard.c_str());
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_DIR, wparam, lparam));
    }

    int ComboBoxController::FindString(int indexStart, const tstring& string) const
    {
        WPARAM wparam = static_cast<WPARAM>(indexStart);
        LPARAM lparam = reinterpret_cast<LPARAM>(string.c_str());
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_FINDSTRING, wparam, lparam));
    }

    int ComboBoxController::FindStringExact(int indexStart, const tstring& string) const
    {
        WPARAM wparam = static_cast<WPARAM>(indexStart);
        LPARAM lparam = reinterpret_cast<LPARAM>(string.c_str());
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_FINDSTRINGEXACT, wparam, lparam));
    }

    bool  ComboBoxController::GetComboBoxInfo(ComboBoxInfo& cbi) const
    {        
        cbi = {0};
        cbi.cbSize = sizeof(ComboBoxInfo);
        return User32::GetComboBoxInfo(WINDOW_HANDLE, reinterpret_cast<PCOMBOBOXINFO>(&cbi)) ? true : false;
    }

    int ComboBoxController::GetCount() const
    {
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETCOUNT, 0, 0));
    }

    int ComboBoxController::GetCurSel() const
    {
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETCURSEL, 0, 0));
    }

    Rect ComboBoxController::GetDroppedControlRect() const
    {
        Rect rc;
        LPARAM lparam = reinterpret_cast<LPARAM>(&rc);
        User32::SendMessageT(WINDOW_HANDLE, CB_GETDROPPEDCONTROLRECT, 0, lparam);
        return rc;
    }

    bool ComboBoxController::GetDroppedState() const
    {
        return static_cast<BOOL>(User32::SendMessageT(WINDOW_HANDLE, CB_GETDROPPEDSTATE, 0, 0));
    }

    int ComboBoxController::GetDroppedWidth() const
    {
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETDROPPEDWIDTH, 0, 0));
    }

    void* ComboBoxController::GetEditCtrl() const
    {        
        COMBOBOXINFO cbi = {0};
        cbi.cbSize = sizeof(cbi);
        User32::GetComboBoxInfo(WINDOW_HANDLE, &cbi);
        return cbi.hwndItem;
    }

    unsigned long ComboBoxController::GetEditSel() const
    {
        return static_cast<DWORD>(User32::SendMessageT(WINDOW_HANDLE, CB_GETEDITSEL, 0, 0));
    }

    bool ComboBoxController::GetExtendedUI() const
    {
        return static_cast<BOOL>(User32::SendMessageT(WINDOW_HANDLE, CB_GETEXTENDEDUI, 0, 0));
    }

    int ComboBoxController::GetHorizontalExtent() const
    {
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETHORIZONTALEXTENT, 0, 0));
    }

    unsigned long ComboBoxController::GetItemData(int index) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        return static_cast<DWORD>(User32::SendMessageT(WINDOW_HANDLE, CB_GETITEMDATA, wparam, 0));
    }

    int ComboBoxController::GetItemHeight(int index) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETITEMHEIGHT, wparam, 0));
    }

    void* ComboBoxController::GetLBCtrl() const
    {
        COMBOBOXINFO cbi = {0};
        cbi.cbSize = sizeof(cbi);
        User32::GetComboBoxInfo(WINDOW_HANDLE, &cbi);
        return cbi.hwndList;
    }

    int ComboBoxController::GetLBText(int index, tstring& text) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        LPARAM lparam = reinterpret_cast<LPARAM>(text.c_str());
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETLBTEXT, wparam, lparam));
    }

    int ComboBoxController::GetLBTextLen(int index) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETLBTEXTLEN, wparam, 0));
    }

    unsigned long ComboBoxController::GetLocale() const
    {
        return static_cast<LCID>(User32::SendMessageT(WINDOW_HANDLE, CB_GETLOCALE, 0, 0));
    }

    int ComboBoxController::GetTopIndex() const
    {
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_GETTOPINDEX, 0, 0));
    }

    int ComboBoxController::InitStorage(int items, int byteCount) const
    {
        WPARAM wparam = static_cast<WPARAM>(items);
        LPARAM lparam = static_cast<LPARAM>(byteCount);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_INITSTORAGE, wparam, lparam));
    }

    int ComboBoxController::InsertString(int index, const tstring& string) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        LPARAM lparam = reinterpret_cast<LPARAM>(string.c_str());
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_INSERTSTRING, wparam, lparam));
    }

    void ComboBoxController::LimitText(int maxChars) const
    {
        WPARAM wparam = static_cast<WPARAM>(maxChars);
        User32::SendMessageT(WINDOW_HANDLE, CB_LIMITTEXT, wparam, 0);
    }

    void ComboBoxController::Paste() const
    {
        User32::SendMessageT(WINDOW_HANDLE, WM_PASTE, 0, 0);
    }

    void ComboBoxController::ResetContent() const
    {
        User32::SendMessageT(WINDOW_HANDLE, CB_RESETCONTENT, 0, 0);
    }

    int ComboBoxController::SelectString(int startAfter, const tstring& string) const
    {
        WPARAM wparam = static_cast<WPARAM>(startAfter);
        LPARAM lparam = reinterpret_cast<LPARAM>(string.c_str());
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SELECTSTRING, wparam, lparam));
    }

    int ComboBoxController::SetCurSel(int index) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SETCURSEL, wparam, 0));
    }

    int ComboBoxController::SetDroppedWidth(int width) const
    {
        WPARAM wparam = static_cast<WPARAM>(width);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SETDROPPEDWIDTH, wparam, 0));
    }

    bool ComboBoxController::SetEditSel(int startChar, int endChar) const
    {
        LPARAM lparam = MAKELONG(startChar, endChar);
        return static_cast<BOOL>(User32::SendMessageT(WINDOW_HANDLE, CB_SETEDITSEL, 0, lparam));
    }

    int ComboBoxController::SetExtendedUI(bool isExtended) const
    {
        WPARAM wparam = static_cast<WPARAM>(isExtended);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SETEXTENDEDUI, wparam, 0));
    }

    void ComboBoxController::SetHorizontalExtent(unsigned int extent ) const
    {
        WPARAM wparam = static_cast<WPARAM>(extent);
        User32::SendMessageT(WINDOW_HANDLE, CB_SETHORIZONTALEXTENT, wparam, 0);
    }

    int ComboBoxController::SetItemData(int index, unsigned long itemData) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        LPARAM lparam = static_cast<LPARAM>(itemData);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SETITEMDATA, wparam, lparam));
    }

    int ComboBoxController::SetItemHeight(int index, unsigned int cyItemHeight) const
    {
        WPARAM wparam = static_cast<WPARAM>(index);
        LPARAM lparam = static_cast<LPARAM>(cyItemHeight);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SETITEMHEIGHT, wparam, lparam));
    }

    unsigned long ComboBoxController::SetLocale(unsigned long newLocale ) const
    {
        WPARAM wparam = static_cast<WPARAM>(newLocale);
        return static_cast<LCID>(User32::SendMessageT(WINDOW_HANDLE, CB_SETLOCALE, wparam, 0));
    }

    int ComboBoxController::SetTopIndex(int index) const
    {
        return 0;
        WPARAM wparam = static_cast<WPARAM>(index);
        return static_cast<int>(User32::SendMessageT(WINDOW_HANDLE, CB_SETTOPINDEX, wparam, 0));
    }

    void ComboBoxController::ShowDropDown(bool show) const
    {
        WPARAM wparam = static_cast<WPARAM>(show);
        User32::SendMessageT(WINDOW_HANDLE, CB_SHOWDROPDOWN, wparam, 0);
    }

}}}//qor::platform::win

#undef WINDOW_HANDLE