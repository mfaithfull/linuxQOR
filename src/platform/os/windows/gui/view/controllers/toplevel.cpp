// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#include "src/configuration/configuration.h"

#include "toplevel.h"
#include "../messages.h"

namespace qor{ namespace platform { namespace nswindows{ namespace gui{ namespace view{
    
    TopLevelWindowController::TopLevelWindowController() : InteractiveWindowController()
    {
    }

    bool TopLevelWindowController::ProcessMessage(Window& window, long long& lResult, unsigned int msg, unsigned long long wParam, long long lParam)
    {
        bool bProcessed = ProcessHook(window, lResult, msg, wParam, lParam);

        if (!bProcessed)
        {
            switch (msg)
            {

            case wmEnterIdle:
            {
                lResult = window.DefWindowProcT(msg, wParam, lParam);
                Window w(PrimitiveHandle((void*)(lParam)));
                OnEnterIdle(static_cast< unsigned short>(wParam), w);
                bProcessed = true;
            }
            break;

            case wmSysCommand:
            {
                window.DefWindowProcT(msg, wParam, lParam);
                unsigned short wYPos = 0;
                unsigned short wXPos = LoWord(lParam);
                unsigned short wData = HiWord(lParam);
                bool bAcceleratorUsed = (wData == -1) ? true : false;
                bool bMnemonic = (wData == 0) ? true : false;
                if (!bAcceleratorUsed && !bMnemonic)
                {
                    wYPos = wData;
                }
                OnSysCommand(wParam, wYPos, wXPos, bAcceleratorUsed, bMnemonic);
                lResult = 0;
                bProcessed = true;
            }
            break;

            case wmDropFiles:
            {
                void* hDropInfo = reinterpret_cast<void*>(wParam);
                OnDropFiles(hDropInfo);
                lResult = 0;
                bProcessed = true;
            }
            break;

            case wmQueryEndSession:
            {
                bool bLoggingOff = (lParam & EndSessionLogOff) > 0 ? true : false;
                lResult = OnQueryEndSession(bLoggingOff) ? true : false;
                bProcessed = true;
            }
            break;

            case wmEndSession:
            {
                bool bEnd = wParam ? true : false;
                bool bLoggingOff = (lParam & EndSessionLogOff) > 0 ? true : false;
                OnEndSession(bEnd, bLoggingOff);
                lResult = 0;
                bProcessed = true;
            }
            break;

            case wmQueryNewPalette:
            {
                lResult = OnQueryNewPalette() ? true : false;
                window.DefWindowProcT(msg, wParam, lParam);
                bProcessed = true;
            }
            break;

            case wmPaletteChanged:
            {
                window.DefWindowProcT(msg, wParam, lParam);
                Window w(PrimitiveHandle((void*)(wParam)));
                OnPaletteChanged(w);
                bProcessed = true;
            }
            break;
            
            case wmCommand:
            {
                unsigned short wNotify = HiWord(wParam);
                unsigned short wID = LoWord(wParam);
                OnCommand(window, wNotify, wID, lParam);
                lResult = 0;
                bProcessed = true;
            }
            break;
            case wmActivateApp:
            {
                bool bActivate = wParam ? true : false;
                unsigned long dwThreadID = static_cast< unsigned long >(lParam);
                OnActivateApp(bActivate, dwThreadID);
                lResult = 0;
                bProcessed = true;
            }
            break;

            case wmDeviceChange:
            {
                lResult = window.DefWindowProcT(msg, wParam, lParam);
                bProcessed = true;
            }
            break;

            default:
                bProcessed = InteractiveWindowController::ProcessMessage(window, lResult, msg, wParam, lParam);
            }
        }

        if(!bProcessed)
        {
            lResult = window.DefWindowProcT(msg, wParam, lParam);
        }
        return bProcessed;
    }

    void TopLevelWindowController::OnEnterIdle(unsigned short wWhy, Window& who)
    {
    }

    void TopLevelWindowController::OnSysCommand(unsigned long long nID, unsigned short wYPos, unsigned short wXPos, bool bAcceleratorUsed, bool bMnemonic)
    {
    }

    void TopLevelWindowController::OnDropFiles(void* hDropInfo)
    {
    }

    bool TopLevelWindowController::OnQueryEndSession(bool bLoggingOff)
    {
        return true;
    }
    
    void TopLevelWindowController::OnEndSession(bool bEnd, bool bLoggingOff)
    {
    }

    bool TopLevelWindowController::OnQueryNewPalette()
    {
        return false;
    }

    void TopLevelWindowController::OnPaletteChanged(Window& changeSource)
    {
    }
    
    void TopLevelWindowController::OnCommand(Window& Window, unsigned short wNotify, unsigned short wID, long long lParam)
    {
    }

    void TopLevelWindowController::OnActivateApp(bool bActivate, unsigned long threadId)
    {
    }


}}}}}//qor::platform::nswindows::gui::view
