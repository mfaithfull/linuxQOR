// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_WINDOWS_GUI_PERTHREAD
#define QOR_PP_H_WINDOWS_GUI_PERTHREAD

#include <map>

#include "src/qor/objectcontext/anyobject.h"
#include "src/framework/thread/currentthread.h"
#include "src/qor/injection/typeidentity.h"
#include "src/qor/factory/factory.h"
#include "src/qor/instance/instance.h"
#include "src/qor/reference/ref.h"
#include "src/qor/reference/newref.h"
#include "src/qor/instance/threadsingleton.h"

#include "window.h"
#include "controllers/i_m/menu.h"
#include "gdiobjects/devicecontext.h"
#include "controllers/i_m/monitor.h"

namespace qor{ namespace platform { namespace nswindows{ 

    class qor_pp_module_interface(QOR_WINGUI) PerThread final
    {
    public:

        PerThread() = default;
        ~PerThread() = default;

        bool AddWindow(Window* window);
        bool RemoveWindow(Window* window);
        Window* FromHandle(void* hWnd);

        bool AddMenu(Menu* menu);
        bool RemoveMenu(Menu* menu);
        Menu* MenuFromHandle(void* hMenu);

        bool AddDeviceContext(ref_of<DeviceContext>::type dc);
        bool RemoveDeviceContext(ref_of<DeviceContext>::type dc);
        ref_of<DeviceContext>::type DeviceContextFromHandle(void* hDC);

        bool AddMonitor(Monitor* mon);
        bool RemoveMonitor(Monitor* mon);
        Monitor* MonitorFromHandle(void* hMon);

        void SetWindowEnumProc(wndenumproc proc);
        wndenumproc GetWindowEnumProc();

        void SetPropertyEnumProc(propenumproc proc);
        propenumproc GetPropertyEnumProc();

        void SetWindowCreationInProgress(ref_of<Window>::type window);
        ref_of<Window>::type GetWindowCreationInProgress();

        unsigned int NextResourceId();
        
    private:

        ref_of<Window>::type m_inProgress;
        wndenumproc m_windowEnumProc;
        propenumproc m_properrtyEnumProc;
        std::map<void*, Window*> m_windowHandleMap;
        std::map<void*, Menu*> m_menuHandleMap;
        std::map<void*, ref_of<DeviceContext>::type> m_deviceContextHandleMap;
        std::map<void*, Monitor*> m_monitorHandleMap;
        unsigned int m_nextResourceId{100};
    };
}}}

namespace qor{ qor_pp_declare_instancer_of(qor::platform::nswindows::PerThread, ThreadSingletonInstancer);}

qor_pp_export extern qor::detail::ThreadInstanceHolder<qor::platform::nswindows::PerThread>* GetCurrentPerThread();

#endif//QOR_PP_H_WINDOWS_GUI_PERTHREAD