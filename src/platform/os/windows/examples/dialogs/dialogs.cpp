// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#include "src/configuration/configuration.h"
#include "src/platform/os/windows/exebootstrap/winqorexeboot.h"
#include "src/platform/os/windows/gui/windows/messagebox.h"
#include "src/platform/os/windows/gui/windows/dialog.h"
#include "src/platform/os/windows/gui/factories/windowstyle.h"

using namespace qor;
using namespace qor::platform::nswindows;
using namespace qor::platform::nswindows::gui::view;
using namespace std;

void messageBoxProc(HelpInfo* helpInfo)
{

}

int main()
{
    int result = MessageBox(nullptr, L"This is a Test", L"Test", MBOKCANCEL|MBHELP|MBDEFBUTTON2|MBICONINFORMATION, 6500).Result();

    MsgBoxParams params;
    params.hInstance = GetInstance();
    params.hwndOwner = nullptr;
    params.lpszText = L"Press F1 to hit help callback.";
    params.lpszCaption = L"Message Box Indirect.";
    params.dwStyle = MBOK;
    params.dwContextHelpId = 0;
    params.dwLanguageId = 0;
    params.lpszIcon = nullptr;
    params.lpfnMsgBoxCallback = &messageBoxProc;
    
    result = MessageBox(params).Result();

    alignas(4) std::byte buffer[1024];
    memset(buffer, 0xffff, 1023);
    DlgTemplate* dlgtmplt = new (buffer) DlgTemplate();    

    // Define a dialog box.
    dlgtmplt->dwExtendedStyle = 0;
    dlgtmplt->style = 0x94c800c4 | DSMODALFRAME | DS3DLOOK | DSSETFONT;
    dlgtmplt->cdit = 1; // 1 Button
    dlgtmplt->x = 0;
    dlgtmplt->y = 0;
    dlgtmplt->cx = 300;
    dlgtmplt->cy = 180;    
 
    unsigned short* pw = (unsigned short*)(dlgtmplt + 1);
    *pw++ = 0;             // No menu
    *pw++ = 0;             // Predefined dialog box class (by default)
    
    wchar_t* pwsz = (wchar_t*)pw;
    int nchar = Dialog::WriteString("Debug",pwsz);
    pw += nchar;
    *pw++ = 10;             //pointsize;
    pwsz = (wchar_t*)pw;
    nchar = Dialog::WriteString("MS Sans Serif",pwsz);//wchar_t     typeface[1];                            
    pw += nchar;

    //-----------------------
    // Define an OK button.
    //-----------------------
    
    pw = Dialog::WAlign(pw, sizeof(unsigned long));    // Align DLGITEMTEMPLATE on DWORD boundary
    DlgItemTemplate* pdit = (DlgItemTemplate*)pw;    
    pdit->x  = 10; pdit->y  = 70;
    pdit->cx = 80; pdit->cy = 20;
    pdit->id = 1;       // OK button identifier
    pdit->style = 0x40000000 | 0x10000000 | BSDEFPUSHBUTTON;
    pdit->dwExtendedStyle = 0;

    pw = (unsigned short*)(pdit + 1);
    *pw++ = 0xFFFF;
    *pw++ = 0x0080;        // Button class

    pwsz = (wchar_t*)pw;
    nchar = Dialog::WriteString("&Apply", pwsz);
    pw += nchar;
    *pw++ = 0;             // No creation data
    pw = Dialog::WAlign(pw, 4);

    /*
    //-----------------------
    // Define a Help button.
    //-----------------------
    pw = Dialog::WAlign(pw);    // Align DLGITEMTEMPLATE on DWORD boundary
    pdit = (DlgItemTemplate*)pw;
    pdit->x  = 55; pdit->y  = 10;
    pdit->cx = 40; pdit->cy = 20;
    pdit->id = 150;    // Help button identifier
    pdit->style = 0x40000000 | 0x10000000 | BSPUSHBUTTON;

    pw = (unsigned short*)(pdit + 1);
    *pw++ = 0xFFFF;
    *pw++ = 0x0080;        // Button class atom

    pwsz = (wchar_t*)pw;
    nchar = 1 + Dialog::WriteString("Help", pwsz);
    pw += nchar;
    *pw++ = 0;             // No creation data
    /*
    //-----------------------
    // Define a static text control.
    //-----------------------
    lpw = lpwAlign(lpw);    // Align DLGITEMTEMPLATE on DWORD boundary
    lpdit = (LPDLGITEMTEMPLATE)lpw;
    lpdit->x  = 10; lpdit->y  = 10;
    lpdit->cx = 40; lpdit->cy = 20;
    lpdit->id = ID_TEXT;    // Text identifier
    lpdit->style = WS_CHILD | WS_VISIBLE | SS_LEFT;

    lpw = (LPWORD)(lpdit + 1);
    *lpw++ = 0xFFFF;
    *lpw++ = 0x0082;        // Static class

    for (lpwsz = (LPWSTR)lpw; *lpwsz++ = (WCHAR)*lpszMessage++;);
    lpw = (LPWORD)lpwsz;
    *lpw++ = 0;             // No creation data
    */
#define DLGTITLE  L"Debug"
#define DLGFONT   L"MS Sans Serif"
#define DLGAPPLY  L"&Apply"
#define DLGCANCEL L"&Cancel"
#define NUMCHARS(aa) (sizeof(aa)/sizeof((aa)[0]))
#define IDC_BITMAP 9

#pragma pack(push, 4)
static struct { // dltt 

    unsigned long  style; 
    unsigned long  dwExtendedStyle; 
    unsigned short   ccontrols; 
    short  x; 
    short  y; 
    short  cx; 
    short  cy; 
    unsigned short   menu;         // name or ordinal of a menu resource
    unsigned short   windowClass;  // name or ordinal of a window class
    wchar_t  wszTitle[NUMCHARS(DLGTITLE)]; // title string of the dialog box
    short  pointsize;       // only if DS_SETFONT flag is set
    wchar_t  wszFont[NUMCHARS(DLGFONT)];   // typeface name, if DS_SETFONT is set

    // control info
    //
    struct {
       unsigned long  style; 
       unsigned long  exStyle; 
       short  x; 
       short  y; 
       short  cx; 
       short  cy; 
       unsigned short   id; 
       unsigned short   sysClass;       // 0xFFFF identifies a system window class
       unsigned short   idClass;        // ordinal of a system window class
       wchar_t  wszTitle[NUMCHARS(DLGAPPLY)];
       unsigned short   cbCreationData; // bytes of following creation data
//       WORD   wAlign;         // align next control to DWORD boundry.
    } apply;

    struct {
       unsigned long  style; 
       unsigned long  exStyle; 
       short  x; 
       short  y; 
       short  cx; 
       short  cy; 
       unsigned short   id; 
       unsigned short   sysClass;       // 0xFFFF identifies a system window class
       unsigned short   idClass;        // ordinal of a system window class
       wchar_t  wszTitle[NUMCHARS(DLGCANCEL)];
       unsigned short   cbCreationData; // bytes of following creation data
    } cancel;

    struct {
       unsigned long  style; 
       unsigned long  exStyle; 
       short  x; 
       short  y; 
       short  cx; 
       short  cy; 
       unsigned short   id; 
       unsigned short   sysClass;       // 0xFFFF identifies a system window class
       unsigned short   idClass;        // ordinal of a system window class
       wchar_t  wszTitle[1];    // title string or ordinal of a resource
       unsigned short   cbCreationData; // bytes of following creation data
    } bitmap;

   } g_DebugDlgTemplate = {

   0x94c800c4 /*WS_POPUP | WS_VISIBLE | WS_CAPTION | WS_SYSMENU*/  // style  0x94c800c4
   | DSMODALFRAME | DS3DLOOK 
   | DSSETFONT,
   0x0,        // exStyle;
   3,          // ccontrols
   0, 0, 300, 180,
   0,                       // menu: none
   0,                       // window class: none
   DLGTITLE,                // Window caption
   10,                       // font pointsize
   DLGFONT,

      {
        0x50030001 /*WS_CHILD | WS_VISIBLE | WS_TABSTOP | WS_GROUP | BS_DEFPUSHBUTTON*/,   // 0x50030001
        0x4/*WS_EX_NOPARENTNOTIFY*/, // 0x4
        190,160,50,14,
        1/*IDOK*/,
        0xFFFF, 0x0080, // button
        DLGAPPLY, 0,
      },

      {
        0x50010000 /*WS_CHILD | WS_VISIBLE | WS_TABSTOP | BS_PUSHBUTTON*/,    // 0x50010000
        0x4/*WS_EX_NOPARENTNOTIFY*/, // 0x4
        244,160,50,14,
        2/*IDCANCEL*/,
        0xFFFF, 0x0080, // button
        DLGCANCEL, 0,
      },

      {
        0x50020000 /*WS_CHILD | WS_VISIBLE | WS_GROUP | SS_LEFT*/,    // 0x50020000
        0x4/*WS_EX_NOPARENTNOTIFY*/, // 0x4
        6,6,288,8,
        IDC_BITMAP,
        0xFFFF, 0x0082, // static
        L"", 0,
      },
   };

#pragma pack(pop)    
    Dialog d(nullptr, (DlgTemplateEx*)(&g_DebugDlgTemplate));
    Dialog d2(nullptr, (DlgTemplateEx*)dlgtmplt);
}
