// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_LINUX_XSHM
#define QOR_PP_H_LINUX_XSHM

#include <string>

#include "src/platform/os/linux/x/xlib/display.h"
#include "src/platform/os/linux/x/xlib/gc.h"
#include "src/platform/os/linux/x/xlib/image.h"
#include "src/platform/os/linux/x/xlib/visual.h"

//All types on this interface must be portable
namespace qor{ namespace platform { namespace nslinux{ namespace x{
    
    class qor_pp_module_interface(QOR_LINX) Display;

    typedef unsigned long ShmSeg;

    struct qor_pp_module_interface(QOR_LINXSHM) ShmCompletionEvent
    {
        int	type;		        // of event
        unsigned long serial;   // # of last request processed by server
        int send_event;	        // true if this came frome a SendEvent request
        Display *display;	    // Display the event was read from
        Drawable drawable;	    // drawable of request
        int major_code;	        // ShmReqCode
        int minor_code;	        // X_ShmPutImage
        ShmSeg shmseg;	        // the ShmSeg used in the request
        unsigned long offset;   // the offset into ShmSeg used in the request
    };

    struct qor_pp_module_interface(QOR_LINXSHM) ShmSegmentInfo
    {
        ShmSeg shmseg;	// resource id
        int shmid;		// kernel id
        char *shmaddr;	// address in client
        int readOnly;	// how the server should attach it
    };

    class qor_pp_module_interface(QOR_LINXSHM) XShm
    {
    public:
    
        XShm(Display* dpy);
        virtual ~XShm();

        int QueryExtension();
        int GetEventBase();
        int QueryVersion(int& majorVersion, int& minorVersion, int& sharedPixmaps);
        int PixmapFormat();
        int Attach(ShmSegmentInfo& shminfo );
        int Detach(ShmSegmentInfo& shminfo );
        int PutImage(unsigned long drawable, GC gc, Image& image, int src_x, int src_y, int dst_x, int dst_y, unsigned int src_width, unsigned int src_height, int send_event);
        int GetImage(unsigned long drawable, Image& image, int x, int y, unsigned long planeMask);
        Image CreateImage(Visual* visual, unsigned int depth, int format, char* data, ShmSegmentInfo& shminfo, unsigned int width, unsigned int height);
        Pixmap CreatePixmap(unsigned long drawable, char* data, ShmSegmentInfo& shminfo, unsigned int width, unsigned int height, unsigned int depth);

    protected:
        Display* m_display;
        ExtCodes m_extCodes;
    };

}}}}//qor::platform::nslinux::x

#endif//QOR_PP_H_LINUX_XSHM

