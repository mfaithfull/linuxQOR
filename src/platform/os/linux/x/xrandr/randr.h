// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_LINUX_XRANDR
#define QOR_PP_H_LINUX_XRANDR

#include <string>
#include <vector>

#include "src/platform/os/linux/x/xlib/display.h"
#include "src/platform/os/linux/x/xlib/gc.h"
#include "src/platform/os/linux/x/xlib/image.h"
#include "src/platform/os/linux/x/xlib/visual.h"
#include "src/platform/os/linux/x/xinput/device.h"

//All types on this interface must be portable
namespace qor{ namespace platform { namespace nslinux{ namespace x{
    
    struct ScreenSize
    {
        int	width, height;
        int	mwidth, mheight;
    };

    typedef unsigned long ModeFlags;

    struct ModeInfo
    {
        unsigned long		id;
        unsigned int	width;
        unsigned int	height;
        unsigned long	dotClock;
        unsigned int	hSyncStart;
        unsigned int	hSyncEnd;
        unsigned int	hTotal;
        unsigned int	hSkew;
        unsigned int	vSyncStart;
        unsigned int	vSyncEnd;
        unsigned int	vTotal;
        char*           name;
        unsigned int	nameLength;
        ModeFlags	    modeFlags;
    };

    struct CrtcGamma
    {
        int		        size;
        unsigned short* red;
        unsigned short* green;
        unsigned short* blue;
    };

    typedef int Fixed;

    struct Transform 
    {
        Fixed  matrix[3][3];
    };    

    struct Panning 
    {
        unsigned long   timestamp;
        unsigned int    left;
        unsigned int    top;
        unsigned int    width;
        unsigned int    height;
        unsigned int    track_left;
        unsigned int    track_top;
        unsigned int    track_width;
        unsigned int    track_height;
        int             border_left;
        int             border_top;
        int             border_right;
        int             border_bottom;
    };

    /*
    struct ProviderResources
    {
        unsigned long timestamp;
        int nproviders;
        unsigned long* providers;
    };*/

    /*
    struct ProviderInfo 
    {
        unsigned int capabilities;
        int ncrtcs;
        unsigned long	*crtcs;
        int noutputs;
        unsigned long    *outputs;
        char	    *name;
        int nassociatedproviders;
        unsigned long *associated_providers;
        unsigned int *associated_capability;
        int		    nameLen;
    };*/


    class qor_pp_module_interface(QOR_LINXRANDR) ScreenConfiguration;
    class qor_pp_module_interface(QOR_LINXRANDR) ScreenResources;
    class qor_pp_module_interface(QOR_LINXRANDR) PropertyInfo;
    class qor_pp_module_interface(QOR_LINXRANDR) Provider;
    class qor_pp_module_interface(QOR_LINXRANDR) MonitorInfo;

    class qor_pp_module_interface(QOR_LINXRANDR) RandR
    {
    public:
    
        RandR(Display* dpy);
        virtual ~RandR();

        int QueryExtension(int& eventBaseReturn, int& errorBaseReturn);
        int QueryVersion(int& majorVersionReturn, int& minorVersionReturn);
        ScreenConfiguration GetScreenInfo(Window window);
        
        //The following are always safe to call, even if RandR is not implemented on a screen
        unsigned short Rotations(int screen, unsigned short& currentRotation);
        std::vector<ScreenSize> Sizes(int screen);
        std::vector<short> Rates(int screen, int sizeID);
        unsigned long Times(int screen, unsigned long& configTimestamp);
        int RootToScreen(Window& root);
        void SelectInput(Window& window, int mask);

        // Version 1.2 additions

        // despite returning a Status, this returns 1 for success
        int GetScreenSizeRange(Window& window, int& minWidth, int& minHeight, int& maxWidth, int& maxHeight);
        void SetScreenSize(Window& window, int width, int height, int mmWidth, int mmHeight);
        ScreenResources GetScreenResources(Window& window);        

        std::vector<unsigned long> ListOutputProperties(unsigned long output);
        PropertyInfo QueryOutputProperty(unsigned long output, unsigned long property);
        void ConfigureOutputProperty(unsigned long output, unsigned long property, int pending, int range, std::vector<long>& values);
        void ChangeOutputProperty(unsigned long output, unsigned long property, unsigned long type, int format, int mode, const std::vector<unsigned char> data);
        void DeleteOutputProperty(unsigned long output, unsigned long property);
        int GetOutputProperty(unsigned long output, unsigned long property, long offset, long length, int _delete, int pending, unsigned long reqType, unsigned long& actualType, int& actualFormat, unsigned long& bytesAfter, std::vector<unsigned char>& value);
        void AddOutputMode(unsigned long output, unsigned long mode);
        void DeleteOutputMode(unsigned long output, unsigned long mode);
        
        // Version 1.3 additions
        void SetOutputPrimary(Window& window, unsigned long output);
        unsigned long GetOutputPrimary(Window& window);
        std::vector<Provider> GetProviderResources(Window& window, unsigned long& timeStamp);
        MonitorInfo GetMonitors(Window& window, int getActive);
        void SetMonitor(Window& window, MonitorInfo& monitorInfo);
        void DeleteMonitor(Window& window, unsigned long name);    

        /* intended to take RRScreenChangeNotify,  or ConfigureNotify (on the root window)
         returns 1 if it is an event type it understands, 0 if not */
        int UpdateConfiguration(Event *event);

    protected:
        Display* m_display;
        ExtCodes m_extCodes;
    };

}}}}//qor::platform::nslinux::x

#endif//QOR_PP_H_LINUX_XRANDR

