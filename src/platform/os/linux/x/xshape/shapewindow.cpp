// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#include "src/configuration/configuration.h"

#include "shapewindow.h"
#include "region.h"
#include "src/platform/os/linux/x/xlib/display.h"
#include "src/platform/os/linux/x/xlib/window.h"
#include "src/platform/os/linux/x/xlib/pixmap.h"
#include "src/platform/os/linux/x/xlib/gc.h"

#include <X11/extensions/shape.h>

namespace qor{ namespace platform { namespace nslinux{ namespace x{

    XShapeWindow::XShapeWindow(Window& window) : m_window(window)
    {
    }

    XShapeWindow::~XShapeWindow()
    {        
    }

    Window& XShapeWindow::XWindow()
    {
        return m_window;
    }

    void XShapeWindow::CombineRegion(int destKind, int xOff, int yOff, XRegion& region,int op)
    {
        XShapeCombineRegion((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), destKind, xOff, yOff, (::Region)(region.Use()), op);
    }

    void XShapeWindow::CombineRectangles(int destKind, int xOff, int yOff, std::vector<Rectangle>& rectangles, int op, int ordering)
    {
        XShapeCombineRectangles((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), destKind, xOff, yOff, reinterpret_cast<XRectangle*>(rectangles.data()), rectangles.size(), op, ordering);
    }

    void XShapeWindow::CombineMask(int destKind, int xOff, int yOff, Pixmap src, int op)
    {
        XShapeCombineMask((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), destKind, xOff, yOff, src.GetId(), op);
    }

    void XShapeWindow::CombineShape(int destKind, int xOff, int yOff, Window src, int srcKind, int op)
    {
        XShapeCombineShape((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), destKind, xOff,yOff, src.GetId(), srcKind, op);
    }

    void XShapeWindow::OffsetShape(int destKind, int xOff, int yOff)
    {
        XShapeOffsetShape((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), destKind, xOff, yOff);
    }

    int XShapeWindow::QueryExtents(int* bounding_shaped, int* x_bounding, int* y_bounding, unsigned int* w_bounding, unsigned int* h_bounding, int* clip_shaped, int* x_clip, int* y_clip, unsigned int* w_clip, unsigned int* h_clip)
    {
        return XShapeQueryExtents((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), bounding_shaped, x_bounding, y_bounding, w_bounding, h_bounding, clip_shaped, x_clip, y_clip, w_clip, h_clip);
    }

    void XShapeWindow::SelectInput(unsigned long mask)
    {
        XShapeSelectInput((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), mask);
    }

    unsigned long XShapeWindow::InputSelected()
    {
        return XShapeInputSelected((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId());
    }

    std::vector<Rectangle> XShapeWindow::GetRectangles(int kind, int ordering)
    {
        std::vector<Rectangle> rectangles;
        int count = 0;
        XRectangle* data = XShapeGetRectangles((::Display*)(m_window.GetDisplay()->Use()), m_window.GetId(), kind, &count, &ordering);
        if( count != 0 && data != nullptr)
        {
            rectangles.resize(count);
            memcpy(rectangles.data(), data, sizeof(Rectangle) * count);
            XFree(data);
        }
        return rectangles;
    }

}}}}//qor::platform::nslinux::x
