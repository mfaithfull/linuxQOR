Internal comms within a process, events within a thread. Signals, events, callbacks, delegates, observation helpers.
The things you need to make event driven applications work that aren't directly related IO.
