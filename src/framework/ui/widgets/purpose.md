Conceptual level widgets just have state and actions
If and how they are rendered is a separate issue.

A Push Button will have a Caption and a Push function that triggers an Action or Enqueue's an event
It will have a transient state indicating whether it is pushed.

The presentation of a Push Button will have a position, a size, a border, styling, colouring, even potentially animation
