base layout item/node
algorithms like flexbox for arranging nodes in a space
vbox
hbox
grid
flexbox
radial
textflow

Layout should be pure logic, not dependent at all on the presentation except perhaps to get the extents of things.
The same layout should be used across all UIs, even minimal as it will determine the left, right, up down, navigation for that as well

