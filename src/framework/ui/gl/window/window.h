// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_GL_WINDOW
#define QOR_PP_H_GL_WINDOW

#include <string>

#include "src/qor/delegate/delegate.h"
#include "../monitor/vidmode.h"
#include "../monitor/monitor.h"
#include "../cursor/cursor.h"
#include "../context/context.h"
#include "../input/input.h"

#undef CreateWindow

#include "windowpimpl.h"

namespace qor{ namespace gl{

    class qor_pp_module_interface(QOR_GL) Window;

    typedef void (* windowposfun)(Window* window, int xpos, int ypos);
    typedef void (* windowsizefun)(Window* window, int width, int height);
    typedef void (* windowclosefun)(Window* window);
    typedef void (* windowrefreshfun)(Window* window);
    typedef void (* windowfocusfun)(Window* window, int focused);
    typedef void (* windowiconifyfun)(Window* window, int iconified);
    typedef void (* windowmaximizefun)(Window* window, int maximized);
    typedef Delegate<void( gl::Window* window, int width, int height )> framebuffersizefun;
    //typedef void (* framebuffersizefun)(Window* window, int width, int height);
    typedef void (* windowcontentscalefun)(Window* window, float xscale, float yscale);

    class qor_pp_module_interface(QOR_GL) Window
    {
        friend class Input;
        friend class Context;
        friend class Ctxconfig;
    public:

        Window() = default;        
        Window(int width, int height, const std::string& title, Monitor* monitor = nullptr, Window* share = nullptr);
        virtual ~Window() = default;

        Window* Create(int width, int height, const std::string& title, Monitor* monitor, Window* share);
        void DefaultHints(void);
        void Hint(int hint, int value);
        void HintString(int hint, const char* value);
        void Destroy();
        int ShouldClose();
        void SetShouldClose(int value);
        std::string GetTitle();
        void SetTitle(const std::string& title);
        void SetIcon(int count, const Image* images);
        void GetPos(int* xpos, int* ypos);
        void SetPos(int xpos, int ypos);
        void GetSize(int* width, int* height);
        void SetSize(int width, int height);
        void SetSizeLimits(int minwidth, int minheight, int maxwidth, int maxheight);
        void SetAspectRatio(int numer, int denom);
        void GetFramebufferSize(int* width, int* height);
        void GetFrameSize(int* left, int* top, int* right, int* bottom);
        void GetContentScale(float* xscale, float* yscale);
        float GetOpacity();
        void SetOpacity(float opacity);
        void Iconify();
        void Restore();
        void Maximize();
        void Show();
        void RequestAttention();
        void Hide();
        void Focus();
        int GetAttrib(int attrib);
        void SetAttrib(int attrib, int value);
        Monitor* GetMonitor();
        void SetMonitor(Monitor* mh, int xpos, int ypos, int width, int height, int refreshRate);
        void SetUserPointer(void* pointer);
        void* GetUserPointer();
        windowposfun SetPosCallback(windowposfun cbfun);
        windowsizefun SetSizeCallback(windowsizefun cbfun);
        windowclosefun SetCloseCallback(windowclosefun cbfun);
        windowrefreshfun SetRefreshCallback(windowrefreshfun cbfun);
        windowfocusfun SetFocusCallback(windowfocusfun cbfun);
        windowiconifyfun SetIconifyCallback(windowiconifyfun cbfun);
        windowmaximizefun SetMaximizeCallback(windowmaximizefun cbfun);
        framebuffersizefun SetFramebufferSizeCallback(framebuffersizefun cbfun);
        keyfun SetKeyCallback(keyfun cbfun);
        windowcontentscalefun SetContentScaleCallback(windowcontentscalefun cbfun);
        void PollEvents();
        void WaitEvents();
        void WaitEventsTimeout(double timeout);
        void PostEmptyEvent();
        void MakeContextCurrent();
        
        //events

        static void InputWindowMonitor(Window* window, Monitor* monitor);
        static void InputWindowCloseRequest(Window* window);
        static void InputWindowDamage(Window* window);
        static void InputWindowContentScale(Window* window, float xscale, float yscale);
        static void InputFramebufferSize(Window* window, int width, int height);
        static void InputWindowMaximize(Window* window, bool maximized);
        static void InputWindowIconify(Window* window, bool iconified);
        static void InputWindowSize(Window* window, int width, int height);
        static void InputWindowPos(Window* window, int x, int y);
        static void InputWindowFocus(Window* window, bool focused);

        Context             context;

        bool Resizeable();
        bool Decorated();
        bool AutoIconify();
        bool Floating();        
        Cursor* GetCursor();
        int GetMinHeight();
        int GetMaxHeight();
        int GetMinWidth();
        int GetMaxWidth();
        int GetNumer();
        int GetDenom();
        VidMode GetVideoMode();
        int GetCursorMode();
        int GetRawMouseMotion();

    protected:

        class Window* next;

        // Window settings and state
        bool           m_resizable;
        bool           m_decorated;
        bool           m_autoIconify;
        bool           m_floating;
        int            focusOnShow;
        int            mousePassthrough;
        int            shouldClose;
        void*          userPointer;
        int            doublebuffer;
        VidMode        m_videoMode;
        Monitor*       m_monitor;
        Cursor*        m_cursor;
        std::string    title;

        int            m_minwidth, m_minheight;
        int            m_maxwidth, m_maxheight;
        int            m_numer, m_denom;

        int            stickyKeys;
        int            stickyMouseButtons;
        int            lockKeyMods;
        int            disableMouseButtonLimit;
        int            m_cursorMode;
        char                mouseButtons[Input::MOUSE_BUTTON_LAST + 1];
        char                keys[KEY_LAST + 1];
        // Virtual cursor position when cursor is disabled
        double              virtualCursorPosX, virtualCursorPosY;
        int            m_rawMouseMotion;

        

        struct {
            windowposfun          pos;
            windowsizefun         size;
            windowclosefun        close;
            windowrefreshfun      refresh;
            windowfocusfun        focus;
            windowiconifyfun      iconify;
            windowmaximizefun     maximize;
            framebuffersizefun    fbsize;
            windowcontentscalefun scale;
            mousebuttonfun        mouseButton;
            cursorposfun          cursorPos;
            cursorenterfun        cursorEnter;
            scrollfun             scroll;
            keyfun                key;
            charfun               character;
            charmodsfun           charmods;
            dropfun               drop;
        } callbacks;


    private:
        
        ref_of<WindowPimpl>::type m_impl;

    };
}}//qor::gl

#endif//QOR_PP_H_GL_WINDOW