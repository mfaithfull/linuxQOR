// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#include "src/configuration/configuration.h"
#include "src/qor/tdd/assert/assert.h"
#include "platform.h"
#include "window/window.h"
#include "cursor/cursor.h"
#include "image.h"
#include "input/joystick.h"
#include "monitor/monitor.h"
#include "context/context.h"

namespace qor{ namespace gl {

    Platform::Platform()
    {
        m_impl = new_ref<PlatformImpl>();
    }

    Platform::~Platform()
    {
    
    }

    //input
    void Platform::GetCursorPos(Window* window, double* x,double* y)
    {
        m_impl->GetCursorPos(window, x, y);
    }

    void Platform::SetCursorPos(Window* window, double xpos, double ypos)
    {
        m_impl->SetCursorPos(window, xpos, ypos);
    }

    void Platform::SetCursorMode(Window* window, int mode)
    {
        m_impl->SetCursorMode(window, mode);        
    }
    
    void Platform::SetRawMouseMotion(Window* window, int enabled)
    {
        m_impl->SetRawMouseMotion(window, enabled);
    }
    
    bool Platform::RawMouseMotionSupported(void)
    {
        return m_impl->RawMouseMotionSupported();
    }

    int Platform::CreateCursor(Cursor* cursor,const Image* image, int xhot, int yhot)
    {
        return m_impl->CreateCursor(cursor, image, xhot, yhot);
    }

    int Platform::CreateStandardCursor(Cursor* cursor,int shape)
    {
        return m_impl->CreateStandardCursor(cursor, shape);
    }

    void Platform::DestroyCursor(Cursor* cursor)
    {
        m_impl->DestroyCursor(cursor);
    }

    void Platform::SetCursor(Window* window, Cursor* cursor)
    {
        m_impl->SetCursor(window, cursor);
    }

    const char* Platform::GetScancodeName(int scancode)
    {
        return m_impl->GetScancodeName(scancode);
    }

    int Platform::GetKeyScancode(int key)
    {
        return m_impl->GetKeyScancode(key);
    }

    void Platform::SetClipboardString(const char* string)
    {
        m_impl->SetClipboardString(string);
    }

    const char* Platform::GetClipboardString(void)
    {
        return m_impl->GetClipboardString();
    }

    int Platform::InitJoysticks(void)
    {
        return m_impl->InitJoysticks();
    }

    void Platform::TerminateJoysticks(void)
    {
        m_impl->TerminateJoysticks();
    }

    int Platform::PollJoystick(Joystick* js, int mode)
    {
        return m_impl->PollJoystick(js, mode);
    }

    const char* Platform::GetMappingName(void)
    {
        return m_impl->GetMappingName();
    }

    void Platform::UpdateGamepadGUID(char* guid)
    {
        m_impl->UpdateGamepadGUID(guid);
    }

    
    //monitor
    void Platform::FreeMonitor(Monitor* monitor)
    {
        m_impl->FreeMonitor(monitor);
    }

    void Platform::GetMonitorPos(Monitor* monitor, int* xpos, int* ypos)
    {
        m_impl->GetMonitorPos(monitor, xpos, ypos);
    }

    void Platform::GetMonitorContentScale(Monitor* monitor, float* xscale, float* yscale)
    {
        m_impl->GetMonitorContentScale(monitor, xscale, yscale);
    }

    void Platform::GetMonitorWorkarea(Monitor* monitor, int* xpos, int* ypos, int* width, int* height)
    {
        m_impl->GetMonitorWorkarea(monitor, xpos, ypos, width, height);
    }

    VidMode* Platform::GetVideoModes(Monitor* monitor, int* count)
    {
        return m_impl->GetVideoModes(monitor, count);
    }

    int Platform::GetVideoMode(Monitor* monitor, VidMode* vidmode)
    {
        return m_impl->GetVideoMode(monitor, vidmode);
    }

    int Platform::GetGammaRamp(Monitor* monitor, GammaRamp* gammaramp)
    {
        return m_impl->GetGammaRamp(monitor, gammaramp);
    }

    void Platform::SetGammaRamp(Monitor* monitor,const GammaRamp* gammaramp)
    {
        m_impl->SetGammaRamp(monitor, gammaramp);
    }

    
    // window
    int Platform::CreateWindow(Window* window, const Wndconfig* wndConfig, const Ctxconfig* ctxConfig, const FBconfig* fbConfig)
    {
        return m_impl->CreateWindow(window, wndConfig, ctxConfig, fbConfig);
    }

    void Platform::DestroyWindow(Window* window)
    {
        m_impl->DestroyWindow(window);
    }

    void Platform::SetWindowTitle(Window* window, const std::string& title)
    {
        m_impl->SetWindowTitle(window, title);
    }

    void Platform::SetWindowIcon(Window* window, int count, const Image* images)
    {
        m_impl->SetWindowIcon(window, count, images);
    }

    void Platform::GetWindowPos(Window* window, int* xpos, int* ypos)
    {
        m_impl->GetWindowPos(window, xpos, ypos);
    }

    void Platform::SetWindowPos(Window* window, int xpos, int ypos)
    {
        m_impl->SetWindowPos(window, xpos, ypos);
    }

    void Platform::GetWindowSize(Window* window, int* width, int* height)
    {
        m_impl->GetWindowSize(window, width, height);
    }

    void Platform::SetWindowSize(Window* window, int width, int height)
    {
        m_impl->SetWindowSize(window, width, height);
    }

    void Platform::SetWindowSizeLimits(Window* window , int minWidth, int minHeight, int maxWidth, int maxHeight)
    {
        m_impl->SetWindowSizeLimits(window, minWidth, minHeight, maxWidth, maxHeight);
    }

    void Platform::SetWindowAspectRatio(Window* window, int numer, int denom)
    {
        m_impl->SetWindowAspectRatio(window, numer, denom);
    }

    void Platform::GetFramebufferSize(Window* window, int* width, int* height)
    {
        m_impl->GetFramebufferSize(window, width, height);
    }

    void Platform::GetWindowFrameSize(Window* window, int* left, int* top, int* right, int* bottom)
    {
        m_impl->GetWindowFrameSize(window, left, top, right, bottom);
    }

    void Platform::GetWindowContentScale(Window* window, float* xscale, float* yscale)
    {
        m_impl->GetWindowContentScale(window, xscale, yscale);
    }

    void Platform::IconifyWindow(Window* window)
    {
        m_impl->IconifyWindow(window);
    }

    void Platform::RestoreWindow(Window* window)
    {
        m_impl->RestoreWindow(window);
    }

    void Platform::MaximizeWindow(Window* window)
    {
        m_impl->MaximizeWindow(window);
    }

    void Platform::ShowWindow(Window* window)
    {
        m_impl->ShowWindow(window);
    }

    void Platform::HideWindow(Window* window)
    {
        m_impl->HideWindow(window);
    }

    void Platform::RequestWindowAttention(Window* window)
    {
        m_impl->RequestWindowAttention(window);
    }

    void Platform::FocusWindow(Window* window)
    {
        m_impl->FocusWindow(window);
    }

    void Platform::SetWindowMonitor(Window* window, Monitor* monitor,int xpos,int ypos, int width, int height, int refreshRate)
    {
        m_impl->SetWindowMonitor(window, monitor, xpos, ypos, width, height, refreshRate);
    }

    int Platform::WindowFocused(Window* window)
    {
        return m_impl->WindowFocused(window);
    }

    int Platform::WindowIconified(Window* window)
    {
        return m_impl->WindowIconified(window);
    }

    int Platform::WindowVisible(Window* window)
    {
        return m_impl->WindowVisible(window);
    }

    int Platform::WindowMaximized(Window* window)
    {
        return m_impl->WindowMaximized(window);
    }

    int Platform::WindowHovered(Window* window)
    {
        return m_impl->WindowHovered(window);
    }

    bool Platform::FramebufferTransparent(Window* window)
    {
        return m_impl->FramebufferTransparent(window);
    }

    float Platform::GetWindowOpacity(Window* window)
    {
        return m_impl->GetWindowOpacity(window);
    }

    void Platform::SetWindowResizable(Window* window, int value)
    {
        m_impl->SetWindowResizable(window, value);
    }

    void Platform::SetWindowDecorated(Window* window, int value)
    {
        m_impl->SetWindowDecorated(window, value);
    }

    void Platform::SetWindowFloating(Window* window, int value)
    {
        m_impl->SetWindowFloating(window, value);
    }

    void Platform::SetWindowOpacity(Window* window, float opacity)
    {
        m_impl->SetWindowOpacity(window, opacity);
    }

    void Platform::SetWindowMousePassthrough(Window* window, int value)
    {
        m_impl->SetWindowMousePassthrough(window, value);
    }

    void Platform::PollEvents(void)
    {
        m_impl->PollEvents();
    }

    void Platform::WaitEvents(void)
    {
        m_impl->WaitEvents();
    }

    void Platform::WaitEventsTimeout(double timeout)
    {
        m_impl->WaitEventsTimeout(timeout);
    }

    void Platform::PostEmptyEvent(void)
    {
        m_impl->PostEmptyEvent();
    }

    // EGL
    //virtual EGLenum getEGLPlatform(EGLint**);
    //virtual EGLNativeDisplayType getEGLNativeDisplay(void);
    //virtual EGLNativeWindowType getEGLNativeWindow(Window*);
    // vulkan
    void Platform::GetRequiredInstanceExtensions(std::vector<std::string>& extensions)
    {
        m_impl->GetRequiredInstanceExtensions(extensions);
    }

    void Platform::PlatformInitTimer(void)
    {
        m_impl->PlatformInitTimer();
    }
    
    uint64_t Platform::PlatformGetTimerValue(void)
    {
        return m_impl->PlatformGetTimerValue();
    }

    uint64_t Platform::PlatformGetTimerFrequency(void)
    {
        return m_impl->PlatformGetTimerFrequency();
    }

    bool Platform::PlatformCreateTls(TLS* tls)
    {
        return m_impl->PlatformCreateTls(tls);
    }

    void Platform::PlatformDestroyTls(TLS* tls)
    {
        m_impl->PlatformDestroyTls(tls);
    }

    void* Platform::PlatformGetTls(TLS* tls)
    {
        return m_impl->PlatformGetTls(tls);
    }

    void Platform::PlatformSetTls(TLS* tls, void* value)
    {
        m_impl->PlatformSetTls(tls, value);
    }

}}//qor::gl

