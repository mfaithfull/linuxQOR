// Copyright Querysoft Limited 2008 - 2025
//
// Permission is hereby granted, free of charge, to any person or organization
// obtaining a copy of the software and accompanying documentation covered by
// this license (the "Software") to use, reproduce, display, distribute,
// execute, and transmit the Software, and to prepare derivative works of the
// Software, and to permit third-parties to whom the Software is furnished to
// do so, all subject to the following:
// 
// The copyright notices in the Software and this entire statement, including
// the above license grant, this restriction and the following disclaimer,
// must be included in all copies of the Software, in whole or in part, and
// all derivative works of the Software, unless such copies or derivative
// works are solely in the form of machine-executable object code generated by
// a source language processor.
// 
// THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
// IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
// FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
// SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
// FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
// ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
// DEALINGS IN THE SOFTWARE.

#ifndef QOR_PP_H_GL_SESSION
#define QOR_PP_H_GL_SESSION

#include "window/wndconfig.h"
#include "context/ctxconfig.h"
#include "platform.h"
#include "input/joystick.h"
#include "tls.h"
#include "constants.h"

namespace qor{ namespace gl{

    typedef void (* glproc)(void);
    typedef void* (* allocatefun)(size_t size, void* user);
    typedef void* (* reallocatefun)(void* block, size_t size, void* user);
    typedef void (* deallocatefun)(void* block, void* user);

    typedef void (* joystickfun)(int jid, int event);
    typedef void (* monitorfun)(Monitor* monitor, int event);

    typedef void (__stdcall * PFN_vkVoidFunction)(void);

    //typedef PFN_vkVoidFunction (__stdcall * PFN_vkGetInstanceProcAddr)(VkInstance,const char*);
    //typedef VkResult (__stdcall * PFN_vkEnumerateInstanceExtensionProperties)(const char*,uint32_t*,VkExtensionProperties*);

    typedef void (*glproc)(void);
    typedef void* OSMesaContext;
    typedef void (*OSMESAproc)(void);

    typedef OSMesaContext (__stdcall * PFN_OSMesaCreateContextExt)(unsigned int, int, int, int, OSMesaContext);
    typedef OSMesaContext (__stdcall * PFN_OSMesaCreateContextAttribs)(const int*,OSMesaContext);
    typedef void (__stdcall * PFN_OSMesaDestroyContext)(OSMesaContext);
    typedef int (__stdcall * PFN_OSMesaMakeCurrent)(OSMesaContext,void*,int,int,int);
    typedef int (__stdcall * PFN_OSMesaGetColorBuffer)(OSMesaContext,int*,int*,int*,void**);
    typedef int (__stdcall * PFN_OSMesaGetDepthBuffer)(OSMesaContext,int*,int*,int*,void**);
    typedef glproc (__stdcall * PFN_OSMesaGetProcAddress)(const char*);

    typedef unsigned int (__stdcall * PFN_eglGetConfigAttrib)(void*,void*,int,int*);
    typedef unsigned int (__stdcall * PFN_eglGetConfigs)(void*,void**,int,int*);
    typedef void* (__stdcall * PFN_eglGetDisplay)(void*);
    typedef int (__stdcall * PFN_eglGetError)(void);
    typedef unsigned int (__stdcall * PFN_eglInitialize)(void*,int*,int*);
    typedef unsigned int (__stdcall * PFN_eglTerminate)(void*);
    typedef unsigned int (__stdcall * PFN_eglBindAPI)(unsigned int);
    typedef void* (__stdcall * PFN_eglCreateContext)(void*,void*,void*,const int*);
    typedef unsigned int (__stdcall * PFN_eglDestroySurface)(void*,void*);
    typedef unsigned int (__stdcall * PFN_eglDestroyContext)(void*,void*);
    typedef void* (__stdcall * PFN_eglCreateWindowSurface)(void*,void*,void*,const int*);
    typedef void* (__stdcall * PFN_eglCreatePbufferSurface)(void*,void*,const int*);
    typedef unsigned int (__stdcall * PFN_eglMakeCurrent)(void*,void*,void*,void*);
    typedef unsigned int (__stdcall * PFN_eglSwapBuffers)(void*,void*);
    typedef unsigned int (__stdcall * PFN_eglSwapInterval)(void*,int);
    typedef const char* (__stdcall * PFN_eglQueryString)(void*,int);
    typedef glproc (__stdcall * PFN_eglGetProcAddress)(const char*);
    typedef void* (__stdcall * PFNEGLGETPLATFORMDISPLAYEXTPROC)(unsigned int,void*,const int*);
    typedef void* (__stdcall * PFNEGLCREATEPLATFORMWINDOWSURFACEEXTPROC)(void*,void*,void*,const int*);


    struct Allocator
    {
        allocatefun allocate;       // The memory allocation function.  See @ref GLFWallocatefun for details about allocation function.
        reallocatefun reallocate;   // The memory reallocation function.  See @ref GLFWreallocatefun for details about reallocation function.        
        deallocatefun deallocate;   // The memory deallocation function.  See @ref GLFWdeallocatefun for details about deallocation function.
        void* user;                 // The user pointer for this custom allocator.  This value will be passed to the allocator functions.
    };

    struct Error
    {
        Error*          next;
        int             code;
        char            description[1024];
    };

    struct qor_pp_module_interface(QOR_GL) SessionConfig
    {
        int           hatButtons;
        int           angleType;
        int           platformID;
        //PFN_vkGetInstanceProcAddr vulkanLoader;
        struct {
            int       menubar;
            int       chdir;
        } ns;
        struct {
            int       xcbVulkanSurface;
        } x11;
        struct {
            int       libdecorMode;
        } wl;
    };


    class qor_pp_module_interface(QOR_GL) Platform;

    class qor_pp_module_interface(QOR_GL) Session
    {
        friend class Monitor;
        friend class Input;
        friend class Context;
        friend class Window;

    public:

        Session();
        virtual ~Session();

        void WindowHint(int hint, int value);
        void DefaultWindowHints();

        //int Init(void)
        //void Terminate(void)
        //void InitHint(int hint, int value)
        //InitAllocator(const Allocator* allocator)
        //InitVulkanLoader(PFN_vkGetInstanceProcAddr loader)
        //GetVersion(int* major, int* minor, int* rev)
        //GetError(const char** description)
        //errorfun SetErrorCallback(errorfun cbfun)

        Platform& GetPlatform()
        {
            return m_platform(qor_shared);
        }

        bool GetJoysticksInitialized()
        {
            return joysticksInitialized;
        }

        void SetJoystickInitialized(bool init)
        {
            joysticksInitialized = init;
        }

        int GetMappingCount()
        {
            return mappingCount;
        }        

        //Events

        static void InputError(int code, const char* format, ...);

    //private:

        bool                initialized;
        Allocator           allocator;
        ref_of<Platform>::type    m_platform;

        struct 
        {
            SessionConfig   init;
            FBconfig        framebuffer;
            Wndconfig       window;
            Ctxconfig       context;
            int             refreshRate;
        } hints;

        Error*              errorListHead;
        Cursor*             cursorListHead;
        Window*             windowListHead;

        Monitor**           monitors;
        int                 monitorCount;

        bool                joysticksInitialized;
        Joystick            joysticks[Joystick::JOYSTICK_LAST + 1];
        Mapping*            mappings;
        int                 mappingCount;

        TLS                 errorSlot;
        TLS                 contextSlot;
        RecursiveMutex      errorLock;

        struct //TODO: extract into a platform specific smart reference
        {
            uint64_t        offset;
            // This is defined in platform.h
            //GLFW_PLATFORM_LIBRARY_TIMER_STATE
            uint64_t        frequency;
        } timer;

        struct 
        {
            unsigned int    platform;
            void*           display;
            int             major, minor;
            int             prefix;

            int             KHR_create_context;
            int             KHR_create_context_no_error;
            int             KHR_gl_colorspace;
            int             KHR_get_all_proc_addresses;
            int             KHR_context_flush_control;
            int             EXT_client_extensions;
            int             EXT_platform_base;
            int             EXT_platform_x11;
            int             EXT_platform_wayland;
            int             EXT_present_opaque;
            int             ANGLE_platform_angle;
            int             ANGLE_platform_angle_opengl;
            int             ANGLE_platform_angle_d3d;
            int             ANGLE_platform_angle_vulkan;
            int             ANGLE_platform_angle_metal;
            int             MESA_platform_surfaceless;

            void*           handle;

            PFN_eglGetConfigAttrib      GetConfigAttrib;
            PFN_eglGetConfigs           GetConfigs;
            PFN_eglGetDisplay           GetDisplay;
            PFN_eglGetError             GetError;
            PFN_eglInitialize           Initialize;
            PFN_eglTerminate            Terminate;
            PFN_eglBindAPI              BindAPI;
            PFN_eglCreateContext        CreateContext;
            PFN_eglDestroySurface       DestroySurface;
            PFN_eglDestroyContext       DestroyContext;
            PFN_eglCreateWindowSurface  CreateWindowSurface;
            PFN_eglCreatePbufferSurface CreatePbufferSurface;
            PFN_eglMakeCurrent          MakeCurrent;
            PFN_eglSwapBuffers          SwapBuffers;
            PFN_eglSwapInterval         SwapInterval;
            PFN_eglQueryString          QueryString;
            PFN_eglGetProcAddress       GetProcAddress;

            PFNEGLGETPLATFORMDISPLAYEXTPROC GetPlatformDisplayEXT;
            PFNEGLCREATEPLATFORMWINDOWSURFACEEXTPROC CreatePlatformWindowSurfaceEXT;
        } egl;

        struct 
        {
            void*           handle;

            PFN_OSMesaCreateContextExt      CreateContextExt;
            PFN_OSMesaCreateContextAttribs  CreateContextAttribs;
            PFN_OSMesaDestroyContext        DestroyContext;
            PFN_OSMesaMakeCurrent           MakeCurrent;
            PFN_OSMesaGetColorBuffer        GetColorBuffer;
            PFN_OSMesaGetDepthBuffer        GetDepthBuffer;
            PFN_OSMesaGetProcAddress        GetProcAddress;

        } osmesa;

        struct 
        {
            int             available;
            void*           handle;
            char*           extensions[2];
            //PFN_vkGetInstanceProcAddr GetInstanceProcAddr;
            int             KHR_surface;
            int             KHR_win32_surface;
            int             MVK_macos_surface;
            int             EXT_metal_surface;
            int             KHR_xlib_surface;
            int             KHR_xcb_surface;
            int             KHR_wayland_surface;
            int             EXT_headless_surface;
        } vk;

        struct 
        {
            monitorfun  monitor;
            joystickfun joystick;
        } callbacks;

        // These are defined in platform.h
        //GLFW_PLATFORM_LIBRARY_WINDOW_STATE
        //GLFW_PLATFORM_LIBRARY_CONTEXT_STATE
        //GLFW_PLATFORM_LIBRARY_JOYSTICK_STATE

        //size_t _glfwEncodeUTF8(char* s, uint32_t codepoint)
        //char** _glfwParseUriList(char* text, int* count)
        //char* _glfw_strdup(const char* source)
        //int _glfw_min(int a, int b)
        //int _glfw_max(int a, int b)
        static void* alloc(size_t count, size_t size);
        static void* realloc(void* block, size_t size);
        static void free(void* block);

        static void terminate(void);
        static void* defaultReallocate(void* block, size_t size, void* user);
        static void defaultDeallocate(void* block, void* user);
        static void* defaultAllocate(size_t size, void* user);
    };

    extern qor_pp_module_interface(QOR_GL) Session& TheSession(void);

}}

#endif//QOR_PP_H_GL_SESSION