Intends to provide generic properties and property collections, convertable to and from equivalent structs by reflection
A property is like a variant of a property map and property vector a property value and recursively of a property itself
