Provides extensible base classes and infrastructure for issue.
issues can be raised and are handled via a Flyer mechanism.
Different types of issues can created which will be handled by
different classes of Flyers
Each Issue gathers context of where and when it was raised.
The What element is left to the derived issue type to determine.
